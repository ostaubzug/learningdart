//final is only initialized when accessed
//const all are also final but it is initialized during compilation
//final seems to be better(?)

void main() {
  //defining types such as String is optional
  final cityName = "Mumbai";
  const PI = 3.14;

  print(PI);
  print(cityName);
}
